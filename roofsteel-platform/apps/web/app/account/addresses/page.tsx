// SCAFFOLD — Phase 3. No Address endpoints exist yet — see Gap Analysis I Section 2.
// Build the backend CRUD before this page can be more than a static shell.
export default function AddressesPage() {
  return (
    <div className="container" style={{ padding: "32px 16px" }}>
      <h1>Saved Addresses</h1>
      <p style={{ color: "var(--steel)", fontSize: 14, marginTop: 8 }}>
        This page is scaffolded, not yet built — see the TODO comment above this component.
      </p>
    </div>
  );
}
