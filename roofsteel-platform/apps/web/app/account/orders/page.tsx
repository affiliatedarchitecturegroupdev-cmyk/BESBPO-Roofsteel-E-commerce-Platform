// SCAFFOLD — Phase 3. List orders for the authenticated account.
// Backend: extend apps/api/src/orders to add a real list-by-account endpoint
// (currently only get-by-orderNumber exists).
export default function OrderHistoryPage() {
  return (
    <div className="container" style={{ padding: "32px 16px" }}>
      <h1>Order History</h1>
      <p style={{ color: "var(--steel)", fontSize: 14, marginTop: 8 }}>
        This page is scaffolded, not yet built — see the TODO comment above this component.
      </p>
    </div>
  );
}
