// SCAFFOLD — Phase 5 (guidelines/12-storefront-ux-and-ia.md's sitemap).
// Transactional legal/info page — NOT a duplicate of the corporate site's
// brand/company content, which stays on the corporate site and gets linked to.
export default function ReturnsPage() {
  return (
    <div className="container" style={{ padding: "32px 16px" }}>
      <h1>Returns & RMA Policy</h1>
      <p style={{ color: "var(--steel)", fontSize: 14, marginTop: 8 }}>
        This page is scaffolded, not yet built — see the TODO comment above this component.
      </p>
    </div>
  );
}
