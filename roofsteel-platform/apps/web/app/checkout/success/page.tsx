// SCAFFOLD — Phase 3. Order confirmation — fetch GET /v1/orders/:orderNumber
// (already real, apps/api/src/orders/orders.module.ts) and display the order summary.
export default function CheckoutSuccessPage() {
  return (
    <div className="container" style={{ padding: "32px 16px" }}>
      <h1>Order Confirmed</h1>
      <p style={{ color: "var(--steel)", fontSize: 14, marginTop: 8 }}>
        This page is scaffolded, not yet built — see the TODO comment above this component.
      </p>
    </div>
  );
}
